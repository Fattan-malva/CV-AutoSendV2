rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /cv/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /landing_usage/{docId} {
      allow create: if true;
      allow read: if true;
    }
  }
}
